## 64.6.4.2.1 USB_USBMODE register

Given that the dual-role controller is initialized in neither host nor device mode, the USB_nUSBMODE register must be programmed for host operation before the EHCI host controller driver can begin EHCI host operations. 
