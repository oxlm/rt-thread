## 61.7.1 USBFS memory mapping

In addition to the address space occupied by the USBFS registers, 512 bytes of embedded RAM are required for use in Keep Alive mode. On this chip the starting address of the USB RAM block is 4801_0000h. 

You can use this USB RAM in Active power mode for BDT entries, USB data buffers, or other purposes. 
